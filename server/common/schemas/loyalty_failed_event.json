{
  "title": "LoyaltyFailedEvent",
  "type": "object",
  "properties": {
    "eventId": { "type": "string" },
    "eventType": { "type": "string" },
    "aggregateId": { "type": "string" },
    "payload": {
      "type": "object",
      "properties": {
        "sagaId": { "type": "string" },
        "reason": { "type": "string" }
      },
      "required": ["sagaId", "reason"]
    }
  },
  "required": ["eventId", "eventType", "aggregateId", "payload"]
}
