{
  "enforce_context_rules": true,
  "rules": [
    "Always route API calls through gateway service at http://localhost:8081",
    "Use Gradle multi-project microservices: gateway, auth, content, media, saga",
    "Prefer JWT in HttpOnly cookies for SSR contexts",
    "Follow domain-driven frontend structure under src/features/*",
    "Avoid adding Maven modules; use Gradle for backend services"
  ]
}
